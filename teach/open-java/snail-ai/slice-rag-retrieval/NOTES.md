# NOTES

- 本主题遵守 L2 垂直切片范围：先用一节短课建立对话到检索注入的主链路，再把 DTO、配置字段、pipeline 顺序、存储边界和 checklist 放入参考文档。
- 用户要求只写入 `teach/open-java/snail-ai/slice-rag-retrieval/**`，因此未更新项目级 `index.md`、`_progress.*` 或其他主题目录。
- 课程事实以当前源码为准。官方文档中提到的问题改写默认值、搜索参数默认值等内容如与源码不完全一致，参考文档优先标注源码行为。
