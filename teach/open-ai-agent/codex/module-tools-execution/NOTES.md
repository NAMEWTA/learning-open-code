# 教学笔记：工具定义与执行抽象

- 用户明确要求简体中文输出。
- 本次是 teach-goal 的 L1 主题 worker 产物，短课只覆盖模块职责、架构位置、接口清单、内部分层、关键依赖和最小执行序列。
- 项目级 `index.md`、`_progress.json`、`_progress.md` 被用户列入禁止写入范围，因此未按 teach 默认流程更新。
- 后续 L2 可拆分为：`shell_command/exec_command` 执行链、`apply_patch` 验证与执行链、工具注册与 code mode 暴露策略、exec-server 远端环境。
