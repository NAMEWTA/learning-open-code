# 教学笔记：Agent 系统模块

- 本轮任务单来自 L1-agent-system，前置锚点是 L0 项目导览课。
- 短课目标限定为：判断一个运行时角色配置如何由 builtin、package、user、project 与 settings 覆盖合成。
- lesson 正文最多触及 3 个核心证据文件；管理动作清单、字段表、内置角色列表和外围依赖放入 reference。
- 后续可继续拆 L2 课程：frontmatter 字段细节、agent memory 注入、chain 解析、skill discovery 与 proactive 建议。
- 按用户要求，本轮不修改 `_progress.json`、`_progress.md`、`index.md` 或其他主题目录。
