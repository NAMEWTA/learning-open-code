# 工作笔记

- 用户指定批量模式；主入口为 `lessons/0001-flow-map.html`。
- 链路超过 4 个阶段，已拆为 `0001-flow-map` + `0002-status-wait-complete` 两节短课；长表放入 `reference/async-lifecycle-flow-map.html`。
- 与 L1 `module-runs-execution` 交叉引用；控制面（interrupt/resume/steer）留给后续切片。
