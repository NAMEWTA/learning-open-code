# 教学笔记：Slash 命令与 prompt workflow 模块

- 本主题由 teach-goal L1 worker 生成，只写入 `teach/open-ai-agent/pi-subagents/module-slash-workflows/`。
- 短课保持一个目标：读懂 prompt workflow 从模板发现、命令注册到 slash 执行桥的最短链路。
- 参考页承载长表格：命令清单、prompt 模板清单、frontmatter/runtime 选项、bridge 边界和测试证据。
- 后续 L2 候选：prompt workflow frontmatter/API、slash chain parser、live-state 渲染生命周期、prompt-template bridge 委派、profiles/model 命令到 profiles 模块的跨界链路。
