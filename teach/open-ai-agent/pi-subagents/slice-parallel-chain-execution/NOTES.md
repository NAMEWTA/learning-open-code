# 工作笔记

- 用户偏好批量模式生成 L2 垂直切片；本主题主课为 `lessons/0001-flow-map.html`。
- 源码路径均带 `src/` 前缀（用户指令中的 `runs/` 对应 `src/runs/`）。
- 与 `module-runs-execution` L1 互补：L1 讲三层边界，本切片专讲 chain 内 parallel 与 dynamic fanout。
