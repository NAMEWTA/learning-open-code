# 教学笔记：Runtime 与 persistence

- 本任务来自 teach-goal 的 L1 module goal，只生成 `module-runtime-persistence` 主题内容。
- lesson 只讲一个目标：15 分钟理解 run 生命周期落到哪些 runtime/persistence 边界。
- 主要读者具备 Python 后端基础，但不假设熟悉 LangGraph checkpointer 的内部表结构。
- 长 repository/model/API 清单放入 `reference/runtime-persistence-overview.html`，避免短课变成源码百科。
