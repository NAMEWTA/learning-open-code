# 教学笔记：Skills 系统

- 本次是 teach-goal 的 L1 执行任务，只处理 `L1-module-skills`。
- lesson 只教一个目标：15 分钟理解 skill 如何被发现、激活、装入 prompt 并约束工具。
- 长生命周期、API、测试锚点、内置 public skill 表集中放入 `reference/skills-overview.html`。
- 关联模块边界：Lead agent 已讲 `SkillActivationMiddleware`、`skill_context` 与 allowed-tools policy；Tools/MCP 会并行讲工具加载边界。本模块只保留必要衔接。
