# 工作笔记

- 用户指定源码入口含 `src/app/core/pwa/`，但当前仓库无此目录；课程以实际分散入口为准并在 reference 标注。
- SW 更新检测由 `SyncEffects.triggerSync$` 串联，不在 `features/mobile/` 内。
- 批量模式：首课 `0001-flow-map.html` 用 mermaid sequenceDiagram 建立全链路地图。
