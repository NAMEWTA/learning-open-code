# NOTES

- 课程层级：L2，Goal 为 `L2-today-project-tag-membership-flow`。
- 父级链接：`module-task-domain`；相关 L2：`slice-task-create-flow`。
- 讲解边界：Today 是虚拟标签，普通标签成员来自 `task.tagIds`，Project 视图列表来自 `project.taskIds/backlogTaskIds`，MenuTree 只决定导航树呈现。
- 新的 L3/L4 候选已放入参考页末尾，最终回复也会列出。
