# 教学笔记：Cron 定时任务模块

- 本主题是 AionU teach-goal 第 4 轮的 L1 模块总览，只允许写入 `teach/open-ai-desktop/AionU/module-cron/`。
- 用户要求 lesson 只讲一条 15 分钟主线；长表格、接口清单和完整源码索引放入 `reference/cron-overview.html`。
- 必须保留 L0 回链，并与 `module-common-adapter`、`module-renderer-core`、`module-conversation-runtime` 做交叉链接。
