# 主题笔记

- 本主题是 teach-goal 的 L2 垂直切片，主题目录固定为 `slice-team-run`。
- 用户要求主入口为 `lessons/0001-flow-map.html`，参考文档为 `reference/team-run-flow-map.html`。
- 快照源文件表只使用 `open-ai-desktop/AionU` 内部相对路径，不写目录、通配符、endpoint 或 teach 路径。
- 本主题不更新项目级 `index.md`、`00-index.md`、`_progress.json`、`_progress.md`。
