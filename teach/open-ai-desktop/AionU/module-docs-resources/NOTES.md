# 教学笔记：AionU docs/resources 模块总览

- 本轮是 teach-goal 第 5 轮并行 worker；只写 `teach/open-ai-desktop/AionU/module-docs-resources/`，不修改项目级 `_progress.*`、`index.md` 或源项目。
- 读者默认已经完成 L0 项目总览；本模块 lesson 需要回链 L0，并把长清单分流到 reference。
- 本模块的关键教学判断：`docs/` 是读者意图导航，`examples/` 是扩展生态契约样本，`.aionui/` 是 AI/开发协作契约，`resources/` 是 README/安装器/发布包资源，`public/` 是 renderer/WebUI 运行时静态资源。
- 新发现需要保留：`docs/architecture/`、`docs/specs/`、`schemas/` 当前缺失；`development.md` 和 `scripts/README.md` 有脚本或构建路径漂移迹象。
